import React, { Component } from 'react';
import Card from "./Card";

export default class Deck extends Component {
    render() {
        return (
            <div>
                <Card />
            </div>
        );
    }
}